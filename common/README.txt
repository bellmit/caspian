Folder containing common projects which can be re-used across caspain common services

** Folder Hireachy

common
    | -- utils          # common utility functions
    | -- webfilters     # common jersey filters which can be plugged into the application servers

